query {
  repositoryOwner(login: "octocat") {
    repository(name: "Hello-World") {
      issue(number: 1347) {
        databaseId
        title
        bodyText
        author {
          login
        }
        comments (last: 10) {
         nodes {
           id
           author {
             login
           }
           bodyText
           publishedAt
         }
        }
      }
    }
  }
}
